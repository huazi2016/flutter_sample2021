import 'package:fluro/fluro.dart';
import 'package:flutter/material.dart';

class MyRouters {
  static String root = "/";
  static String routerPage01 = "/router01";
  static void configUerRouter(Router router) {
    //router.noSuchMethod(new Handler(handlerFunc: (BuildContext context, ))))
  }
}
